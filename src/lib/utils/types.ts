export enum RoundMode {
  none = 'none',
  left = 'left',
  right = 'right',
  all = 'all',
 }

export interface EntityNames {
  singular: string,
  plural: string
 }

 export type SelectionType = 'single' | 'multiple'
 